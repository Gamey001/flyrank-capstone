{
  "name": "flyrank-portfolio-site",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run check:amber",
    "preview": "astro preview",
    "check": "astro check",
    "check:amber": "node scripts/check-amber.mjs dist",
    "verify": "astro check && astro build && npm run check:amber"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "sharp": "^0.33.5",
    "@fontsource/space-grotesk": "^5.1.0",
    "@fontsource/inter": "^5.1.0",
    "@fontsource/jetbrains-mono": "^5.1.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.6.3"
  }
}
